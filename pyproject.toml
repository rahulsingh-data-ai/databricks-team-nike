[project]
name = "team-nike-hackathon"
dynamic = ["version"]
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.119.0",
    "pydantic-settings>=2.11.0",
    "uvicorn>=0.37.0",
    "databricks-sdk>=0.74.0",
    "sqlmodel>=0.0.27",
    "psycopg[binary,pool]>=3.2.11",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "ty>=0.0.12",
]

[tool.apx.metadata]
app-name = "team-nike-hackathon"
app-slug = "team_nike_hackathon"
app-entrypoint = "team_nike_hackathon.backend.app:app"
api-prefix = "/api"
metadata-path = "src/team_nike_hackathon/_metadata.py"

[tool.apx.ui]
root = "src/team_nike_hackathon/ui"

[tool.apx.ui.registries]
"@animate-ui" = "https://animate-ui.com/r/{name}.json"
"@ai-elements" = "https://registry.ai-sdk.dev/{name}.json"
"@svgl" = "https://svgl.app/r/{name}.json"

[build-system]
requires = ["hatchling", "uv-dynamic-versioning>=0.7.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
vcs = "git"
fallback-version = "0.0.0"

[tool.hatch.build.hooks.version]
path = "src/team_nike_hackathon/_version.py"
template = '''
version = "{version}"
'''

[tool.hatch.build]
artifacts = ["src/team_nike_hackathon/__dist__", "src/team_nike_hackathon/_metadata.py"]
exclude = ["src/team_nike_hackathon/ui"]
